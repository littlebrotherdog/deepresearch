fastapi>=0.110.0
uvicorn>=0.29.0
openai>=1.30.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pydantic>=2.0.0
# urllib3 v2 requires OpenSSL 1.1.1+; pin <2 to avoid the LibreSSL warning on
# macOS system Python (which ships with LibreSSL 2.x).
urllib3<2
